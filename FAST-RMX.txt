/save.bin: contains the null-terminated string sSavedata (e.g. '73536176656461746100') followed by JSON data.

unlockedVehicles sets the current vehicle unlock progress (out of 15). Harder difficulties can be unlocked (leagues->unlocked) without breaking the cup unlock progression of earlier ones. In general the unlocks system is fairly difficult to break.
